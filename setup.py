from setuptools import setup

setup(
   name='factorial',
   url='https://github.com/j4qfrost/factorial.git',
   version='0.1.0',
   description='',
   author='',
   author_email='',
   modules=['factorial'],
   install_requires=[], #external packages as dependencies
   zip_safe=False
 )